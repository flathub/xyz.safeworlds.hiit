id: xyz.safeworlds.hiit
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: hiit
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --device=dri
modules:
  - name: hiit
    buildsystem: meson
    run-tests: true
    sources:
      - type: git
        url: https://github.com/mfep/exercise-timer
        commit: cd1a1980647774d98b7a8dd6048dcc4f316482c3
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)$
        tag: v1.9.1
